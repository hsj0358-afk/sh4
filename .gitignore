# 비밀값
.env

# 파이썬
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# 생성물
reports/
*.log

# 토토 분석기 생성물
cache/
data/teams.learned.yaml
data/last_round.txt
data/teams.league.yaml

# 회차 분석 저장본 (Phase 4-C). 재실행으로 다시 만들 수 있고 회차당 수백 KB
# 라 커밋하지 않는다 — 축적이 목적인 data/rounds.csv 와 다르다 (§1-6-2).
data/artifacts/

# 1·2단계 분석 결과 보관 (Phase 6-F-3). 채팅에서 다시 받으면 되는 중간
# 작업물이라 커밋하지 않는다 — 원자료인 panel_results/(§1-20)와 다르다.
panel_work/

# 잃어버린 세계의 지도

1897년의 세계 유적지를 배경으로 한 **모바일 텍스트 TRPG**.
플레이어는 GM이 서술하는 미지의 세계를 탐험하며, 직접 1D20을 굴려 사건을 해결하고
잊힌 문명의 진실을 추적한다.

> 「인류는 처음이 아니었다.」

빌드 도구도 의존성도 없다. 순수 ES 모듈 + Node 표준 라이브러리로만 돌아간다.

```bash
npm start        # http://localhost:5173 (같은 네트워크의 휴대폰에서도 열린다)
npm test         # 규칙 · 콘텐츠 무결성 검사 72개
npm run playtest # 무작위 플레이어 수백 회를 굴려 막다른 길과 난이도를 본다
```

---

## 1. 무엇이 구현되어 있나

기획서의 MVP 범위(17절)와 구현 우선순위(19절) 1·2순위를 담았다.

| 기획서 항목 | 상태 |
| --- | --- |
| 캐릭터 생성 · 8직업 · 8능력치 | 구현 |
| 1D20 판정, 5구간 분기 | 구현 |
| 주사위 연출 (굴림 → 지연 → 공개) | 구현 |
| GM 로그 기반 텍스트 진행 | 구현 |
| 선택지 3~5개 | 구현 (콘텐츠 검사로 강제) |
| 자유 입력 행동 해석 | 구현 |
| 인벤토리 · 유물 · 특별 아이템 | 구현 |
| 체력 / 정신력 / 위험도 | 구현 |
| 동료 시스템 (호감도 · 신뢰도 · 부상) | 구현 |
| 첫 번째 지역 (룩소르 / 왕가의 계곡) | 구현 — 9장면, 판정 20종 |
| 메인 미스터리 단서 | 구현 — 9개, 다음 지역 연결 포함 |
| 세이브 / 이어하기 | 구현 |

전투는 기획서 10절의 방향(턴제·텍스트 로그·회피와 협상이 더 중요)을 따르되,
별도 전투 시스템 대신 **다중 라운드 조우 장면**으로 구현했다. 협상 · 매수 · 우회 ·
엄폐 사격 · 도주 · 유물 양도가 모두 서로 다른 결말로 이어진다. 독립된 전투 서브시스템은
지역이 늘어난 뒤에 만드는 편이 낫다고 판단했다 — 지금 만들면 한 장면을 위한 규칙이 된다.

---

## 2. 구조

```
index.html              세로형 단일 화면
src/
  engine/               규칙. DOM 을 모른다.
    rng.js              시드 기반 난수 (재현 가능)
    dice.js             1D20 판정과 5구간 분기
    rules.js            보정치 계산 + 근거 기록
    state.js            상태 모델과 효과 적용
    gm.js               장면 진행 · 판정 요구 · 서사 분기
    freeform.js         자유 입력 해석
    save.js             localStorage
  content/              시나리오. 규칙을 모른다.
    stats.js  professions.js  items.js  companions.js  clues.js
    episodes/luxor.js   에피소드 1
  ui/                   화면. 엔진이 뱉는 이벤트를 그리기만 한다.
    main.js  render.js  panels.js  styles.css  audio.js
tools/
  serve.js              의존성 없는 정적 서버
  playtest.js           자동 플레이테스트
tests/                  node:test
```

**엔진 / 콘텐츠 / UI 를 세 방향으로 갈라 두었다.** 콘텐츠는 상태를 직접 건드리지 않고
effect 객체만 서술한다. UI 는 규칙을 모르고 이벤트만 그린다. 새 지역을 추가할 때
건드릴 파일은 `content/episodes/` 하나다.

---

## 3. 판정 규칙

```
1D20 + 보정 ≥ 목표값        → 성공
목표값 −3 이내로 모자람      → 부분 성공 (대가를 치르고 얻는다)
그 미만                     → 실패
자연 20 / 자연 1            → 목표값과 무관하게 대성공 / 대실패
```

기획서 7-2의 "10~14는 보통·부분 성공" 감각을 유지하면서, 난이도가 다른 판정에도
같은 규칙이 적용되도록 **총합 대비 목표값** 방식으로 일반화했다.

보정치의 출처는 전부 화면에 표시된다.

```
관찰 +4   고고학자 전문 +2   확대경 +2   나디아 하룬 지원 +2
1D20 +10 · 목표값 12
```

- **능력치** — 1~5, 직업별 편향
- **직업 전문** — 직업 태그가 판정 태그와 겹치면 +2
- **장비** — 가장 잘 맞는 **하나만**. 장비를 쌓아 판정을 무너뜨릴 수 없다
- **동료** — 마찬가지로 하나만. 신뢰도가 낮으면 덜 돕고, 부상이면 더 덜 돕는다
- **상태** — 부상 −1 / 중상 −2, 동요 −1 / 공황 −2
- **위험도** — 능력이 아니라 **목표값**을 올린다 (5 이상 +1, 8 이상 +2)

### 실패는 벌이 아니다

모든 실패 분기가 `goto`, 피해, 단서, 아이템, 플래그 중 최소 하나를 동반한다는 것을
테스트로 강제한다(`tests/content.test.js`). 벽을 못 열면 대신 바닥 아래에서 빈 공간이 울린다.

---

## 4. 자유 입력

해석 순서는 좁은 것에서 넓은 것으로 간다.

1. 장면이 직접 정의한 해석기 (`freeform`)
2. 선택지를 말로 부른 경우 (`keys` 대조)
3. 동료 이름 → 관계 장면
4. 소지품 이름 → 사용
5. 전역 행동 사전 (휴식, 수첩, 소지품 확인, 기도, 고함…)
6. 동사만 잡히면 → 장면의 기본 조사 판정 / 이동 안내
7. 해석 실패 → **거절하지 않고 세계관 안에서 되묻는다**

> 당신은 그렇게 하려다가, 이곳에서 그 행동이 무엇이 되어야 할지 알지 못한 채 멈춘다.
> 조금 더 분명하게. — 무엇을 보고, 무엇을 건드릴 것인가?

정신력이 바닥나면 입력을 한 박자 어긋나게 읽는 오독이 섞인다(기획서 7-7).

---

## 5. 콘텐츠를 추가하려면

`src/content/episodes/` 에 파일 하나를 만들고 `scenes` 를 채운다.

```js
scenes: {
  gate: {
    id: 'gate',
    location: '메소포타미아 · 두 강 사이',
    body: (state) => ['...'],          // 배열 또는 (state) => 배열
    revisitBody: ['...'],              // 재방문 시 (선택)
    onEnter: (state, visits) => ({ danger: 1 }),
    ambientCheck: { ... },             // "주변을 살펴본다" 의 기본 판정
    choices: [
      {
        id: 'push',
        label: '석판을 민다',
        keys: ['민다', '석판'],         // 자유 입력에서 이 선택지를 부르는 말
        requires: { items: ['등반 로프'], clues: ['black_sun'], tags: ['잠입'] },
        once: true,
        check: { stat: '체력', tags: ['완력'], target: 13, prompt: ['...'] },
        outcomes: {
          crit:    { text: ['...'], effects: { clues: ['...'] } },
          success: { text: ['...'], effects: { goto: 'inner' } },
          partial: { text: ['...'], effects: { hp: -2, goto: 'inner' } },
          fail:    { text: ['...'], effects: { danger: 1 } },
          fumble:  { text: ['...'], effects: { hp: -4, san: -2, danger: 2 } },
        },
      },
    ],
    freeform: [{ keys: ['냄새'], text: ['...'], effects: {} }],
  },
}
```

`outcomes` 는 다섯 구간을 다 쓰지 않아도 된다. 빠진 구간은 가장 가까운 것으로 대체된다
(대실패 → 실패, 대성공 → 성공).

**effect 객체**: `hp` `san` `danger` `time` `items` `removeItems` `spend`
`clues` `flags` `companions` `companion` `goto` `end`

`npm test` 가 새 콘텐츠의 장면 참조 · 아이템 이름 · 단서 id · 동료 id · 능력치 이름 ·
도달 가능성 · 실패 분기의 대가 유무를 전부 검사한다. 오타는 테스트에서 걸린다.

---

## 6. 검증

- **72개 테스트** — 주사위 분포, 보정 규칙, 상태 클램프, 세션 흐름, 자유 입력, 콘텐츠 무결성
- **자동 플레이테스트** — 무작위 플레이어 400회 기준

```
fumble    5.3%   fail 20.6%   partial 14.8%   success 53.9%   crit 5.4%
에필로그 도달 81.0%      막힌 세션 0
```

무작위로 고르는 플레이어 기준 사망·정신붕괴가 약 19%. 신중한 플레이어는 더 오래 산다.

---

## 7. 다음에 할 것

기획서 19절 3순위.

- 에피소드 2 (메소포타미아) — 에피소드 1이 좌표를 넘겨준다
- 지역 간 캠페인 상태 이월 (단서 · 동료 · 크레인과의 관계)
- 독립 전투 시스템 — 조우 장면이 두 곳 이상 생긴 뒤에
- 다중 엔딩 · 유물 도감 · 난이도 옵션
